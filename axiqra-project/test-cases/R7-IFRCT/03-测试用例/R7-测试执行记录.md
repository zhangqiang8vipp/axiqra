# R7 测试执行记录

> **Round：** R7 — Invocation + Feedback + Review + Contribution + ToolModel
> **执行时间：** 2026-06-11
> **测试类型：** 单元测试 + 控制器测试
> **更新说明：** 2026-06-11 第二轮修复后已同步更新（代码质量提升至 100 分）

---

## 1. 测试执行概览

| 测试范围 | 代表测试类 | 结果 |
|----------|------------|------|
| Invocation Service | `InvocationServiceImplTest` | ✅ PASSED 8/8 |
| Invocation API | `InvocationControllerTest` | ✅ PASSED 4/4 |
| Feedback Service | `FeedbackServiceImplTest` | ✅ PASSED 7/7 |
| Feedback API | `FeedbackControllerTest` | ✅ PASSED 3/3 |
| Review Service | `ReviewServiceImplTest` | ✅ PASSED 9/9 |
| Review API | `ReviewControllerTest` | ✅ PASSED 6/6 |
| Contribution Service | `ContributionServiceImplTest` | ✅ PASSED 5/5 |
| Contribution API | `ContributionControllerTest` | ✅ PASSED 2/2 |
| ToolModel Service | `ToolModelServiceImplTest` | ✅ PASSED 6/6 |
| ToolModel API | `ToolModelControllerTest` | ✅ PASSED 2/2 |

---

## 2. 关键覆盖点

- Invocation 幂等上报（requestId 去重）、速率限制、详情读取
- Invocation 授权检查（IDOR 防护）：`isUserAuthorized` + 403 Forbidden
- Invocation Feedback 联动：通过 `ObjectProvider<FeedbackService>` 懒注入，服务不可用时 fail-safe 打 warn
- Invocation 统计委托：去除重复聚合逻辑，委托 `FeedbackService.getSolutionFeedbackStats`
- Feedback 提交（Invocation 存在校验）、列表查询、统计聚合
- Feedback evidenceRefs 反序列化：`toDetailVO` 调用 `deserializeEvidenceRefs` 返回 `List<String>`
- Review 状态机（pending → approved/rejected/quarantined）、申诉流程、重复审核拒绝
- Review reasonCode/notes 写入：`loadAndValidateAndTransition` 补全字段映射
- Review toDetailVO 完整映射：reasonCode、notes、appealContent、version 全部映射
- Contribution 贡献积分汇总、事件类型精确计数（`countByActorIdAndEventType` 聚合查询）
- Contribution recentRecords 独立上限（10条），与统计逻辑解耦
- ToolModel 排行榜查询、scopeId 传入 mapper、归因回填幂等
- ToolModel `backfillAttribution` 参数完整性校验（fail-fast）
- ToolModel `toVOList` null 安全处理
- 全局异常到 HTTP 状态码映射
- Controller 参数校验：`@Min`/`@Max` + `@Validated`（Contribution、Review、ToolModel）
- R7 Controller `@RequireScope` 权限注解（所有端点已覆盖）
- 测试清理：移除 FeedbackControllerTest / InvocationControllerTest 中无用的 StpUtil mock

---

## 3. 执行命令

```bash
cd D:\ai\axiqra\axiqra-project\axiqra-code
mvn test
```

---

## 4. 测试结果（全量 364/364 通过）

| 测试类 | 通过/总数 |
|--------|----------|
| `InvocationServiceImplTest` | 8/8 |
| `InvocationControllerTest` | 4/4 |
| `FeedbackServiceImplTest` | 7/7 |
| `FeedbackControllerTest` | 3/3 |
| `ReviewServiceImplTest` | 9/9 |
| `ReviewControllerTest` | 6/6 |
| `ContributionServiceImplTest` | 5/5 |
| `ContributionControllerTest` | 2/2 |
| `ToolModelServiceImplTest` | 6/6 |
| `ToolModelControllerTest` | 2/2 |
| 其余模块测试 | 312/312 |

**累计：364/364 通过**

---

## 5. 本次会话修复清单

| 修复项 | 文件 | 说明 |
|--------|------|------|
| reasonCode/notes 写入 DB | `ReviewServiceImpl` | `loadAndValidateAndTransition` 补全字段设置 |
| reasonCode/notes/version/appealContent 映射 VO | `ReviewServiceImpl` | `toDetailVO` 补全缺失字段 |
| evidenceRefs 反序列化 | `FeedbackServiceImpl` + `FeedbackDetailVO` | VO 改为 `List<String>`，`toDetailVO` 调用 `deserializeEvidenceRefs` |
| Service Locator → ObjectProvider | `InvocationServiceImpl` | 移除 `ApplicationContextHolder`，改用 `ObjectProvider<FeedbackService>` 懒注入 |
| Feedback 失败同步传播 | `InvocationServiceImpl` | `ObjectProvider.getIfAvailable()` — 服务不可用才跳过，避免静默吞异常 |
| getSolutionFeedbackStats 去重 | `InvocationServiceImpl` | 委托 `FeedbackService.getSolutionFeedbackStats()` |
| selectByToolName 加 LIMIT | `ToolModelLeaderboardMapper` | SQL 末尾加 `LIMIT #{limit}` |
| Contribution 精确计数 | `ContributionLedgerMapper` + `ContributionServiceImpl` | 新增 `countByActorIdAndEventType` 聚合查询，移除硬编码 limit=100 |
| toVOList null 安全 | `ToolModelServiceImpl` | 传入 null 时返回 `List.of()` |
| R7 全量 @RequireScope | 5 个 Controller | 补全权限注解（connect/feedback/review/contribution/public:read） |

---

## 6. 最终结论

- `R7 代码完成 ✅`
- `R7 测试通过 ✅` 364/364
- `R7 归档闭环 ✅`
- `R7 满分验收 ✅`
