name: Test Workflow

on:
  pull_request:
    types: [opened, synchronize, reopened]
    paths:
      - ".github/workflows/test.yml"
  push:
    branches:
      - fix/openrouter-review-circular-env
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Test passed"
